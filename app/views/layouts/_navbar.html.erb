
<div id="custom-bootstrap-menu" class="navbar navbar-default " role="navigation">
    <div class="container-fluid container">

      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">Be My Chef</a>
      </div>
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <%if signed_in?%>
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to 'Create Event',new_event_path,method: :get %></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%=current_user.first_name%> <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to 'My Profile',user_path(current_user.id),method: :get %></li>
                <li><%= link_to 'My Events',events_path,method: :get %></li>
                    <% if current_user.chef_status == true%>
                      <li><%= link_to 'My Bids',bids_path,method: :get %></li>
                      <%end%>
                    <li role="separator" class="divider"></li>
                    <li class="text-center">
                    <%= link_to 'Sign out', destroy_user_session_path, method: :delete %>
                    </li>
              </ul>
          <li style="top:3px;">
           <%= image_tag current_user.image_url(:pthumb).to_s, class: "avatar img-circle", alt:"avatar" if current_user.image %>
           </li>
            </li>
          </ul>
        <%else %>
          <ul class="nav navbar-nav navbar-right">
            <li><a href="#">Help</a></li>
            <li><%= link_to 'Create Account',new_user_registration_path,method: :get%></li>
            <li><%= link_to 'Log in',user_session_path,method: :post%></li>
            <%end %>
          </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>








